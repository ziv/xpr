# decide

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build decide` to build the library.

## Running unit tests

Run `nx test decide` to execute the unit tests via [Jest](https://jestjs.io).
